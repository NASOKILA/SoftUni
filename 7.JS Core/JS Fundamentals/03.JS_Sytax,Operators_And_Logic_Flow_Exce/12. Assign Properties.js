function solve(args) {

    let nameTag = args[0];
    let obj =
        {
            [args[0]]:args[1],
            [args[2]]:args[3],
            [args[4]]:args[5],
        };

    console.log(obj);
}

solve(['name', 'Pesho', 'age', '23', 'gender', 'male']);











