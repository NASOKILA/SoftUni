<a href="#/flight/{{_id}}" class="added-flight">
    <img src="{{image}}" alt="" class="picture-added-flight">
    <h3>{{destination}}</h3>
    <span>from {{origin}}</span>
    <span>{{departure_date}}</span>
</a>